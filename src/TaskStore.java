import java.util.ArrayList;
import java.util.List;

public class TaskStore {

    // Instance for AcademicTask collection. Encapsulated within scope only.
    private static final List<AcademicTask> AcademicTasks = new ArrayList<>();

    // TODO persistent data handling for text, .csv file of the AcademicTasks collection
}
